# Optional non-secret build-time overrides for the frontend.
# Copy this file to /opt/pokeleximon/app/web.env only if you need overrides.
# Do not put tokens, DSNs, passwords, or admin credentials here.

WEB_SENTRY_ENVIRONMENT=production
WEB_SENTRY_RELEASE=server-managed
WEB_SENTRY_TRACES_SAMPLE_RATE=0
WEB_FEATURE_CONNECTIONS_ENABLED=false
