# Copy this file to /opt/pokeleximon/secrets/proxy.env on the EC2 host.
# Keep permissions at 0600 and ownership root:root.
# Do not commit the copied file.

PROXY_BASIC_AUTH_USER=admin
PROXY_BASIC_AUTH_PASSWORD_HASH=CHANGE_ME_HASHED_ON_SERVER_ONLY
