# Copy this file to /opt/pokeleximon/secrets/monitoring.env on the EC2 host.
# Keep permissions at 0600 and ownership root:root.
# Do not commit the copied file.

GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=CHANGE_ME_ON_SERVER_ONLY
ALERTMANAGER_WEBHOOK_URL=
ALERTMANAGER_WEBHOOK_SEND_RESOLVED=true
